<!-- This is a LOT of nested divs... I'd generally suggest against having this
many for such a small amount of content. Additionally, a table is not the best
element for layout here. Tables should only be used for tabular data, not layout.
Semantic HTML5 elements like main, section, etc would be better -->
<div class="panel panel-default">
  <div class="panel-body">
    <div class="row">
      <div class="col-md-4">
        <%= image_tag @comic.photo_url, class: 'photo_url' %>
        <div class="table-responsive">
          <table class="table">
            <tbody>
              <tr>
                <td><strong>Title:</strong></td>
                <td><%= @comic.title %></td>
              </tr>
              <tr>
                <strong>Desrciption:</strong>
                <%= @comic.desrciption %>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>

<%= link_to 'Edit', edit_comic_path(@comic) %> |
<%= link_to 'Back', comics_path %>
<%= link_to 'Delete', comic_path(@comic), method: :delete %>
